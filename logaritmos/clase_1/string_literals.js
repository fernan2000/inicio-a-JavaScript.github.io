let nombre = "Vasty"
let edad = 19
let materia = "tecnología"

console.log("El profesor de " + materia + " se llama " + nombre + " su edad es: " + edad)

// usando string literals
console.log(`El profesor de ${materia} se llama ${nombre} y su edad es: ${edad}`)

// let str_tradicional 

let str_literal = `Esta línea usa string literals  
como ${nombre} que tiene ${edad} años y enseña ${materia}.`